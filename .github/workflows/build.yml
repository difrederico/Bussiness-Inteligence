name: Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
    
    - name: Install system dependencies
      run: |
        sudo apt update
        sudo apt install -y git zip unzip openjdk-17-jdk autoconf libtool pkg-config zlib1g-dev libncurses5-dev libncursesw5-dev cmake libffi-dev libssl-dev
        sudo apt install -y build-essential libltdl-dev python3-dev
    
    - name: Install Python dependencies
      run: |
        pip install --upgrade pip
        pip install cython==0.29.36
        pip install buildozer
        pip install kivy

    - name: Clean Buildozer cache
      run: |
        rm -rf ~/.buildozer/android/platform/android-ndk-r*
        rm -rf .buildozer
        echo "Cache limpo para forçar download do NDK 25b"

    - name: Build APK with Buildozer (Simple Version)
      timeout-minutes: 60
      run: |
        buildozer android debug
        
    - name: Upload APK artifact
      uses: actions/upload-artifact@v4
      with:
        name: LeitorQR-Fiscal-APK
        path: bin/*.apk
        
    - name: Create Release
      if: github.ref == 'refs/heads/main'
      uses: softprops/action-gh-release@v2
      with:
        tag_name: v1.0.${{ github.run_number }}
        name: Leitor QR Fiscal v1.0.${{ github.run_number }}
        files: bin/*.apk
        body: |
          🚀 **Leitor QR Fiscal - Versão Android**
          
          📱 **APK Pronto para Instalação**
          
          **Como Instalar:**
          1. Baixe o arquivo APK
          2. Ative "Fontes Desconhecidas" no Android
          3. Instale o APK
          4. Permita acesso à câmera
          
          **Funcionalidades:**
          ✅ Leitura de QR Codes fiscais
          ✅ Validação de chaves de acesso
          ✅ Histórico de leituras
          ✅ Exportação CSV
          ✅ Interface mobile otimizada
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}